@model IEnumerable<FileInfo>
<div class="container">
    <div class="row">
        <div class="col-md-12">
            <table class="table">
                <thead>
                    <tr>
                        <th>编号</th>
                        <th>标题</th>
                        <th>添加时间</th>
                        <th>操作</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var x in Model)
                    {
                        <tr>
                            <td>@x.Id</td>
                            <td>@x.Title</td>
                            <td>@x.CreateTime</td>
                            <td><a href="@x.Path" class="btn btn-success">右键另存为下载</a></td>
                        </tr>

                    }
                </tbody>
            </table>
        </div>
    </div>
    <div class="row">
        <div class="col-md-3">
            <button class="btn btn-primary form-control" id="code">添加源代码</button>
        </div>
    </div>
</div>
<div class="modal fade" id="createcode">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                <h4 class="modal-title">添加源代码</h4>
            </div>
            <div class="modal-body">
                <form asp-action="CreateSourceCode" asp-controller="Home" asp-route-id="@User.Current.Id" method="post" id="codefrm" enctype="multipart/form-data">
                    <table class="table">
                        <tbody>
                            <tr>
                                <td>
                                    <input type="text" name="filename" class="form-control" placeholder="请输入源代码标题" />
                                </td>
                            </tr>
                            <tr>
                                <td>
                                    <input type="file" name="file" formenctype="multipart/form-data" />
                                </td>
                            </tr>
                            <tr>
                                <td class="text-danger">源代码打包成ZIP后再上传</td>
                            </tr>
                        </tbody>
                    </table>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-danger" data-dismiss="modal">关闭</button>
                <button type="button" class="btn btn-primary" id="send">提交</button>
            </div>
        </div>
    </div>
</div>
<script src="~/scripts/jquery-2.1.1.js"></script>
<script type="text/javascript">
    $("#code").click(function () {
        $("#createcode").modal("show");
    });
    $("#send").click(function () {
        $("#codefrm").submit();

    });
</script>
